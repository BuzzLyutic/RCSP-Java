# 🌊 Практическая работа №3: Реактивное программирование с RxJava

![Java](https://img.shields.io/badge/Java-17+-ED8B00?style=for-the-badge&logo=openjdk&logoColor=white)
![RxJava](https://img.shields.io/badge/RxJava-3.x-B7178C?style=for-the-badge&logo=reactivex&logoColor=white)
![Reactive](https://img.shields.io/badge/Reactive-Programming-FF6B6B?style=for-the-badge)

## Описание

Практическая работа посвящена изучению **реактивного программирования** с использованием библиотеки **RxJava**. Включает 4 задания различной сложности.

---

## Содержание заданий

| № | Задание | Сложность | Ключевые концепции |
|---|---------|-----------|-------------------|
| **1** | Система мониторинга датчиков | ⭐⭐ | Observable, Observer |
| **2** | Операции с потоками | ⭐ | filter, concat, take |
| **3** | Социальная сеть друзей | ⭐⭐ | flatMap, fromArray |
| **4** | Обработка файлов | ⭐⭐⭐ | Buffer, schedulers, backpressure |

---

## Задание 1: Система мониторинга датчиков

### Описание

Реализовать систему мониторинга с датчиками и сигнализацией.


### Логика сигнализации

```
┌─────────────────────────────────────────────┐
│ Условие              │ Реакция             │
├──────────────────────┼─────────────────────┤
│ Temp > 25            │ ⚠️ Warning: Temp!   │
│ CO2 > 70             │ ⚠️ Warning: CO2!    │
│ Temp > 25 AND CO2>70 │ 🚨 ALARM!!!         │
│ Оба в норме          │ ✅ All OK           │
└─────────────────────────────────────────────┘
```

---

## Задание 2: Операции с потоками

### 2.1 Фильтрация чисел

**Задача:** Преобразовать поток из 1000 случайных чисел (0-1000) в поток, содержащий только числа > 500.

```
Input:  [234, 567, 123, 789, 345, 901, ...]
         ↓ filter(x -> x > 500)
Output: [567, 789, 901, ...]
```

**Операторы:** `filter()`

---

### 2.2 Объединение потоков

**Задача:** Даны два потока по 1000 элементов. Сформировать поток, обрабатывающий оба последовательно.

```
Stream 1: [1, 2, 3]
Stream 2: [4, 5, 6]
           ↓ concat()
Result:   [1, 2, 3, 4, 5, 6]
```

**Операторы:** `concat()` / `concatWith()`

---

### 2.3 Ограничение количества элементов

**Задача:** Дан поток из 10 случайных чисел. Получить только первые 5.

```
Input:  [45, 12, 78, 34, 56, 89, 23, 67, 91, 43]
         ↓ take(5)
Output: [45, 12, 78, 34, 56]
```

**Операторы:** `take()`


---

## Задание 3: Социальная сеть друзей

### Описание

Реализовать систему для работы с друзьями пользователей.


### Требования

- ✅ Класс `UserFriend` с полями `userId`, `friendId`
- ✅ Массив заполнен случайными данными
- ✅ Функция `Observable<UserFriend> getFriends(int userId)`
- ✅ Использование `Observable.fromArray()`
- ✅ Преобразование через `flatMap()`

### Пример данных

```
UserFriend[] friends = {
    {userId: 1, friendId: 2},
    {userId: 1, friendId: 3},
    {userId: 2, friendId: 4},
    {userId: 3, friendId: 1},
    ...
}

Input:  [1, 2, 3]
         ↓ flatMap(id -> getFriends(id))
Output: [UserFriend{1,2}, UserFriend{1,3}, UserFriend{2,4}, ...]
```

### Ключевые операторы

| Оператор | Применение |
|----------|------------|
| `fromArray()` | Создание Observable из массива |
| `flatMap()` | Преобразование каждого элемента в Observable |
| `filter()` | Фильтрация друзей по userId |

---

## Задание 4: Система обработки файлов

### Описание

Реализовать систему генерации и обработки файлов с очередью.


### Характеристики компонентов

#### 📄 Файл

| Параметр | Значение |
|----------|----------|
| **Тип** | XML, JSON, XLS |
| **Размер** | 10-100 (случайное) |

#### 🏭 Генератор файлов

| Параметр | Значение |
|----------|----------|
| **Задержка генерации** | 100-1000 мс (случайная) |
| **Тип файла** | Случайный (XML/JSON/XLS) |

#### 📦 Очередь

| Параметр | Значение |
|----------|----------|
| **Вместимость** | 5 файлов |
| **Стратегия** | Backpressure (при переполнении) |

#### ⚙️ Обработчик файлов

| Параметр | Формула/Значение |
|----------|------------------|
| **Поддерживаемый тип** | Один из: XML, JSON, XLS |
| **Время обработки** | `Размер файла × 7 мс` |

---

## ✅ Контрольный список

### Задание 1
- [ ] Датчик температуры генерирует данные каждую секунду
- [ ] Датчик CO2 генерирует данные каждую секунду
- [ ] Сигнализация корректно обрабатывает оба датчика
- [ ] Используются Observable и Observer
- [ ] Выводятся корректные сообщения

### Задание 2
- [ ] 2.1: Фильтрация работает корректно
- [ ] 2.2: Потоки объединяются последовательно
- [ ] 2.3: Берутся только первые 5 элементов
- [ ] Используются операторы RxJava

### Задание 3
- [ ] Класс UserFriend реализован
- [ ] Массив заполнен случайными данными
- [ ] Функция getFriends() возвращает Observable
- [ ] Используется Observable.fromArray()
- [ ] Используется flatMap для преобразования

### Задание 4
- [ ] Генератор создает файлы с задержкой
- [ ] Очередь ограничена 5 элементами
- [ ] Обработчики фильтруют по типу файла
- [ ] Время обработки = size × 7ms
- [ ] Система работает асинхронно

---

## 🎓 Результаты обучения

После выполнения всех заданий вы:

- ✅ Освоите основы **реактивного программирования**
- ✅ Научитесь работать с **Observable** и **Observer**
- ✅ Поймете работу основных **операторов RxJava**
- ✅ Научитесь работать с **асинхронными потоками**
- ✅ Реализуете систему с **backpressure**
- ✅ Освоите **многопоточность** в RxJava


---

<div align="center">

**Made with ❤️ using RxJava**

**Think Reactive. Code Reactive.**

</div>
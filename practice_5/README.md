# 🗄️ Практическая работа №5: Распределенная файловая система

![Java](https://img.shields.io/badge/Java-17+-ED8B00?style=for-the-badge&logo=openjdk&logoColor=white)
![Spring Boot](https://img.shields.io/badge/Spring_Boot-3.0+-6DB33F?style=for-the-badge&logo=spring-boot)
![Docker](https://img.shields.io/badge/Docker-2496ED?style=for-the-badge&logo=docker&logoColor=white)
![Maven](https://img.shields.io/badge/Maven-C71A36?style=for-the-badge&logo=apache-maven)

## Описание задания

Разработать **децентрализованную распределенную систему** с использованием **Spring/Spring Boot**.

### Требования

- Загрузка и выгрузка файлов различных форматов
- Развертывание минимум **4 экземпляров** приложения через Docker
- Демонстрация работоспособности при отключении **одного** узла
- Демонстрация работоспособности при отказе **любых двух** узлов
- Децентрализованная архитектура

---

## 💡 Решение

### Тема проекта

**Децентрализованное распределенное файловое хранилище** (Distributed File Storage)

Система представляет собой peer-to-peer сеть, где каждый узел может:

| Функция | Описание |
|---------|----------|
| **Прием файлов** | Принимать и хранить файлы любых форматов |
| **Репликация** | Автоматически реплицировать файлы на другие узлы |
| **Выдача файлов** | Отдавать файлы по запросу пользователей |
| **Отказоустойчивость** | Продолжать работу при отказе других узлов |


---

## Сборка и запуск

### Шаг 1: Сборка JAR файла

#### Вариант 1: IntelliJ IDEA

1. Откройте правую панель **Maven**
2. Разверните **Lifecycle**
3. Двойной клик на **clean**
4. Двойной клик на **package**

#### Вариант 2: Терминал

```bash
mvn clean package -DskipTests
```

#### ✅ Проверка

Убедитесь, что создался файл:

```
target/distributed-file-storage-1.0.0.jar
```

---

### Шаг 2: Запуск Docker Desktop

1. Откройте **Docker Desktop**
2. Дождитесь полного запуска
3. Убедитесь, что иконка Docker в трее **зелёная** 🟢

---

### Шаг 3: Сборка образов и запуск контейнеров

#### Запуск всех 4 узлов

```bash
# Сборка образов и запуск всех 4 узлов
docker-compose up --build
```


#### 🌐 Доступные узлы

| Узел | URL | Порт |
|------|-----|------|
| **Node 1** | http://localhost:8081 | 8081 |
| **Node 2** | http://localhost:8082 | 8082 |
| **Node 3** | http://localhost:8083 | 8083 |
| **Node 4** | http://localhost:8084 | 8084 |

---

## Тестирование системы

### Автоматическое тестирование

```bash
# Сделать скрипт исполняемым
chmod +x test-dfs.sh

# Запустить тесты
./test-dfs.sh

# Или с сохранением лога
./test-dfs.sh 2>&1 | tee test-results.log
```

### 📋 Что делает скрипт

- ✅ Создаёт тестовые файлы
- ✅ Выполняет все curl команды
- ✅ Добавляет паузы после остановки/запуска контейнеров
- ✅ Показывает красивый цветной вывод
- ✅ Сохраняет загруженные файлы для проверки

### 🎯 Сценарии тестирования

#### 1️⃣ Загрузка и репликация файлов

```bash
# Загрузка файла на узел 1
curl -X POST http://localhost:8081/api/files/upload -F "file=@test.txt"

# Проверка наличия файла на всех узлах
curl http://localhost:8081/api/files
curl http://localhost:8082/api/files
curl http://localhost:8083/api/files
curl http://localhost:8084/api/files
```

#### 2️⃣ Скачивание файлов

```bash
# Скачивание с разных узлов
curl http://localhost:8081/api/files/download/test.txt -o downloaded1.txt
curl http://localhost:8082/api/files/download/test.txt -o downloaded2.txt
curl http://localhost:8083/api/files/download/test.txt -o downloaded3.txt
```

#### 3️⃣ Тест отказоустойчивости (1 узел)

```bash
# Остановка узла 1
docker stop dfs-node1

# Загрузка файла на другой узел
curl -X POST http://localhost:8082/api/files/upload -F "file=@test2.txt"

# Проверка доступности
curl http://localhost:8083/api/files/download/test.txt -o test_after_failure.txt
```

#### 4️⃣ Тест отказоустойчивости (2 узла)

```bash
# Остановка двух узлов
docker stop dfs-node1
docker stop dfs-node2

# Проверка работоспособности
curl http://localhost:8083/api/info

# Загрузка нового файла
curl -X POST http://localhost:8083/api/files/upload -F "file=@test3.txt"

# Скачивание с оставшихся узлов
curl http://localhost:8084/api/files/download/test3.txt -o test3_downloaded.txt
```

#### 5️⃣ Восстановление узлов

```bash
# Запуск остановленных узлов
docker start dfs-node1
docker start dfs-node2

# Ожидание синхронизации (5-10 секунд)
sleep 5

# Проверка состояния
curl http://localhost:8081/api/info
```

---

## API Endpoints

### Файловые операции

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `POST` | `/api/files/upload` | Загрузка файла |
| `GET` | `/api/files` | Список всех файлов |
| `GET` | `/api/files/download/{filename}` | Скачивание файла |
| `DELETE` | `/api/files/{filename}` | Удаление файла |

### Информация о системе

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/info` | Информация об узле |
| `GET` | `/api/peers` | Список подключенных узлов |
| `GET` | `/api/health` | Состояние узла |

---

## Остановка системы

```bash
# Остановка и удаление контейнеров
docker-compose down

# Остановка с удалением volumes (полная очистка)
docker-compose down -v
```

---

## Проверка работоспособности

### ✅ Контрольный список

- [ ] Все 4 узла успешно запустились
- [ ] Файл, загруженный на узел 1, доступен на всех узлах
- [ ] При остановке узла 1 система продолжает работать
- [ ] При остановке узлов 1 и 2 система продолжает работать
- [ ] После восстановления узлов происходит синхронизация
- [ ] Файлы различных форматов (txt, jpg, pdf, zip) обрабатываются корректно

### 🔍 Мониторинг логов

```bash
# Просмотр логов всех узлов
docker-compose logs -f

# Просмотр логов конкретного узла
docker-compose logs -f dfs-node1

# Последние 100 строк логов
docker-compose logs --tail=100
```

---

## 🏆 Результаты работы

После выполнения практической работы вы:

- ✅ Создали полностью **децентрализованную** систему
- ✅ Реализовали **автоматическую репликацию** данных
- ✅ Обеспечили **отказоустойчивость** при выходе из строя узлов
- ✅ Освоили работу с **Docker Compose** для multi-container приложений
- ✅ Научились тестировать **распределенные системы**

---

## 📁 Структура проекта

```
distributed-file-storage/
├── src/
│   └── main/
│       ├── java/
│       │   └── com/dfs/
│       │       ├── controller/
│       │       ├── service/
│       │       ├── model/
│       │       └── config/
│       └── resources/
│           └── application.yml
├── docker-compose.yml
├── Dockerfile
├── pom.xml
├── test-dfs.sh
└── README.md
```

---

## 🔧 Технологический стек

| Технология | Назначение |
|------------|------------|
| **Java 17+** | Язык программирования |
| **Spring Boot 3.0+** | Фреймворк приложения |
| **Spring Web** | REST API |
| **Docker** | Контейнеризация |
| **Docker Compose** | Оркестрация контейнеров |
| **Maven** | Сборка проекта |

---

## 💡 Особенности реализации

### Репликация
- Автоматическая при загрузке файла
- Фактор репликации: 3 (файл хранится на 3 узлах)
- Асинхронная передача данных

### Отказоустойчивость
- Peer-to-peer архитектура
- Нет single point of failure
- Автоматическое переключение на доступные узлы

### Масштабируемость
- Легко добавить новые узлы
- Динамическое обнаружение peers
- Балансировка нагрузки

---

## Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что все порты свободны (8081-8084)
3. Проверьте, что Docker Desktop запущен
4. Пересоберите образы: `docker-compose up --build --force-recreate`

---

<div align="center">

**Made with ❤️ using Spring Boot & Docker**

**Distributed. Decentralized. Fault-tolerant.**

</div>